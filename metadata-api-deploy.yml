trigger:
  branches:
    include: [ azure-pipelines ]

pool:
  name: Default

variables:
- group: lib-metadata-api

steps:
- checkout: self

# Setup .NET SDK
- task: UseDotNet@2
  displayName: 'Setup .NET SDK'
  inputs:
    packageType: 'sdk'
    version: '$(DOTNET_VERSION)'

# Cache NuGet
- task: Cache@2
  displayName: 'Cache NuGet packages'
  inputs:
    key: 'nuget | "$(Agent.OS)" | **/BlobsMetadataAPI.csproj'
    restoreKeys: |
      nuget | "$(Agent.OS)"
      nuget
    path: '$(NUGET_PACKAGES)'

# Build & publish API Function
- task: DotNetCoreCLI@2
  displayName: 'Restore'
  inputs:
    command: 'restore'
    projects: "$(PROJECT_PATH)"
    feedsToUse: 'select'

- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: 'build'
    projects: "$(PROJECT_PATH)"
    arguments: '--configuration Release --no-restore'

- task: DotNetCoreCLI@2
  displayName: 'Publish'
  inputs:
    command: 'publish'
    projects: "$(PROJECT_PATH)"
    publishWebProjects: false
    arguments: '--configuration Release --output $(Build.ArtifactStagingDirectory)/publish --no-build'

# (Optional) Set app settings to Key Vault *references* so CI never sees values
- task: AzureCLI@2
  displayName: 'Configure KV references on Function App'
  inputs:
    azureSubscription: "$(AZURE_SUB)"
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      az functionapp config appsettings set \
        --name "$(APP_NAME)" \
        --resource-group "$(RESOURCE_GROUP)" \
        --settings \
          "AzureWebJobsStorage=@Microsoft.KeyVault(SecretUri=$(AzureWebJobsStorage))" \
          "MNSTRG=@Microsoft.KeyVault(SecretUri=$(MNSTRG))" \
          "MTSTRG=@Microsoft.KeyVault(SecretUri=$(MTSTRG))" \
          "FUMSGO=@Microsoft.KeyVault(SecretUri=$(FUMSGO))" \
          "FUMSGI=@Microsoft.KeyVault(SecretUri=$(FUMSGI))" \
          "FUNCSA=@Microsoft.KeyVault(SecretUri=$(FUNCSA))"

# Deploy zip package (no publish profile needed)
- task: AzureFunctionApp@1
  displayName: 'Deploy to Azure Function App'
  inputs:
    azureSubscription: '$(AZURE_SUB)'
    appType: 'functionAppLinux'
    appName: '$(APP_NAME)'
    package: '$(Build.ArtifactStagingDirectory)/publish'